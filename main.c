#include "push_swap.h"

void main(int argc, char **argv)
{
    // argcが適切でない場合、null
    if (argc == 1 || argc >= 3)
        return (NULL);
    // argc == 2の場合、処理を行う

    // ヴァリデーションチェックも行う

    // 数字ごとにセパレート

    // stackに数字を格納

    // ソートさせる

    // ソートがちゃんとできたかヴァリデーションチェック

    return;
}